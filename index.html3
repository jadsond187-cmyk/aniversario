<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Feliz Aniversário Rafaella 🎉</title>

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            height: 100vh;
            background: linear-gradient(135deg, #ff7eb3, #8e44ad);
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            color: white;
        }

        .card {
            background: rgba(255, 255, 255, 0.18);
            backdrop-filter: blur(14px);
            border-radius: 25px;
            padding: 40px;
            width: 380px;
            text-align: center;
            box-shadow: 0 25px 50px rgba(0,0,0,0.3);
            animation: entrada 1.5s ease;
        }

        @keyframes entrada {
            from {
                opacity: 0;
                transform: scale(0.7);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        h1 {
            font-size: 2.2em;
            margin-bottom: 10px;
        }

        h2 {
            font-weight: 300;
            margin-bottom: 20px;
        }

        .mensagem {
            font-size: 1.1em;
            min-height: 90px;
        }

        button {
            margin-top: 30px;
            padding: 14px 30px;
            border: none;
            border-radius: 30px;
            background: #fff;
            color: #8e44ad;
            font-weight: bold;
            font-size: 1em;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            background: #ffe6f2;
            transform: scale(1.05);
        }

        .assinatura {
            margin-top: 20px;
            font-size: 0.9em;
            opacity: 0.8;
        }

        /* Confetes */
        .confete {
            position: absolute;
            width: 10px;
            height: 10px;
            animation: cair linear infinite;
        }

        @keyframes cair {
            0% {
                transform: translateY(-10vh) rotate(0deg);
            }
            100% {
                transform: translateY(110vh) rotate(360deg);
            }
        }
    </style>
</head>

<body>

    <div class="card">
        <h1>🎉 Feliz Aniversário 🎂</h1>
        <h2>Rafaella</h2>

        <div class="mensagem" id="mensagem"></div>

        <button onclick="celebrar()">Celebrar 🎊</button>

        <div class="assinatura">
            — Com carinho, Jadson 💜
        </div>
    </div>

    <script>
        const texto =
            "Que seu dia seja tão especial quanto você. " +
            "Que não faltem sorrisos, alegria, paz e momentos inesquecíveis. " +
            "Que todos os seus sonhos encontrem o caminho certo para se realizar ✨";

        let i = 0;
        function escrever() {
            if (i < texto.length) {
                document.getElementById("mensagem").innerHTML += texto.charAt(i);
                i++;
                setTimeout(escrever, 40);
            }
        }

        escrever();

        function celebrar() {
            criarConfetes();
            alert("🎂 Parabéns, Rafaella! Que seu novo ano seja incrível!");
        }

        function criarConfetes() {
            for (let i = 0; i < 120; i++) {
                const confete = document.createElement("div");
                confete.classList.add("confete");

                confete.style.left = Math.random() * 100 + "vw";
                confete.style.backgroundColor =
                    `hsl(${Math.random() * 360}, 100%, 60%)`;
                confete.style.animationDuration =
                    Math.random() * 3 + 2 + "s";

                document.body.appendChild(confete);

                setTimeout(() => confete.remove(), 5000);
            }
        }
    </script>

</body>